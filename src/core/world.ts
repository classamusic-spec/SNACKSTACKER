/**
 * World-scale constants shared by gameplay and content. Kept in core/ so the
 * geometry kit can reason about how much of a layer has been cut away without
 * importing from game/.
 */

/** Footprint of the plate and the first layer, in world units. */
export const BASE_FOOTPRINT = 2.4;
